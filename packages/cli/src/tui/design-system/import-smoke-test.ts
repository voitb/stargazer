/**
 * Design system import smoke test.
 * Ensures the main index surface can be imported without type errors.
 */

import {
  tokens,
  spacing,
  typography,
  colors,
  ThemeProvider,
  useTheme,
  gradientLine,
  gradientText,
  Logo,
  AnimatedLogo,
  renderLogo,
  StarSpinner,
  Typewriter,
  Pulse,
  Card,
  Divider,
  Badge,
  KeyHint,
  Toast,
  detectTheme,
  getTerminalSize,
} from './index.js';

export const designSystemSmokeTest = {
  tokens,
  spacing,
  typography,
  colors,
  ThemeProvider,
  useTheme,
  gradientLine,
  gradientText,
  Logo,
  AnimatedLogo,
  renderLogo,
  StarSpinner,
  Typewriter,
  Pulse,
  Card,
  Divider,
  Badge,
  KeyHint,
  Toast,
  detectTheme,
  getTerminalSize,
};
