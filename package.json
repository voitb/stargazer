{
  "name": "stargazer-monorepo",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "check": "turbo check",
    "format": "turbo format",
    "test": "turbo test",
    "clean": "turbo clean && rm -rf node_modules",
    "lunamark": "pnpm --filter lunamark dev"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
